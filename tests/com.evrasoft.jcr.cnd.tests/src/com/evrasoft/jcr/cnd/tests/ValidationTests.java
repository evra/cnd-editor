package com.evrasoft.jcr.cnd.tests;

public class ValidationTests {

	// check that
	// ntd.setNotqueryable(!ntd.isQueryable());
	// ntd.setQueryable(ntd.isQueryable());
	// are consistent

	// check that ntd.getOrderable is consistent with JSR ntd sematics ()

	// check orderable flag on cnd (it cannot be exported from repository cnd)

	// check defaults defined by JSR

	// autocreated single value (not multi) property must retrun default values
	// array of size 1

	// duplicate property and nodetypes names (except of multi properties)

	// primary item defined on ntd level and on property/node level must be
	// equal
}
